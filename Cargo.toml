[workspace]
resolver = "2"
members = [
  "server",
  "cli",
  "lib",
  "wasm",
  "plugin-examples/random-folder-extender",
  "plugin-examples/test-plugin",
  "atomic-plugin",
]

# Tauri build is deprecated, see
