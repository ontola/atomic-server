/**
 * Moved into `@tomic/lib` so Store path generation and the app share one slug
 * rule (see `stringToSlug.ts` there). Re-exported here so existing imports
 * keep working.
 */
export { stringToSlug } from '@tomic/lib';
